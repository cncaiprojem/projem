# =================================================================================
# PRODUCTION ENVIRONMENT CONFIGURATION TEMPLATE
# =================================================================================
# KRITIK UYARI: Bu dosya sadece production konfigürasyon şablonudur
# ASLA gerçek production secret'larını bu dosyaya yazmayın!
# Tüm secret değerler external secret management (Doppler, Vault, K8s secrets) ile yönetilmelidir
# =================================================================================

# PRODUCTION UYGULAMA AYARLARI
ENV=production
DEV_AUTH_BYPASS=false
API_PORT=8000
WEB_PORT=3000

# PRODUCTION VERITABANI
# PostgreSQL - Managed service kullanın (AWS RDS, Azure Database vb.)
POSTGRES_USER=${SECRET_POSTGRES_USER}
POSTGRES_PASSWORD=${SECRET_POSTGRES_PASSWORD}
POSTGRES_DB=freecad_production
POSTGRES_HOST=${SECRET_POSTGRES_HOST}
POSTGRES_PORT=5432
DATABASE_URL=${SECRET_DATABASE_URL}

# PRODUCTION CACHE & QUEUE
# Redis - Managed service kullanın (AWS ElastiCache, Azure Cache vb.)
REDIS_URL=${SECRET_REDIS_URL}

# PRODUCTION STORAGE
# S3 veya S3-compatible storage
S3_BUCKET_NAME=production-cnc-artifacts
AWS_ACCESS_KEY_ID=${SECRET_AWS_ACCESS_KEY_ID}
AWS_SECRET_ACCESS_KEY=${SECRET_AWS_SECRET_ACCESS_KEY}
AWS_S3_ENDPOINT=${SECRET_AWS_S3_ENDPOINT}
AWS_S3_REGION=eu-central-1

# PRODUCTION MONİTORİNG
# OpenTelemetry ve Sentry - Production endpoints
OTEL_EXPORTER_OTLP_ENDPOINT=${SECRET_OTEL_ENDPOINT}
OTEL_SERVICE_NAME=freecad-api-prod
SENTRY_DSN=${SECRET_SENTRY_DSN}

# PRODUCTION GÜVENLİK
# Güçlü secret key - HSM veya secure vault'tan alın
SECRET_KEY=${SECRET_JWT_KEY}
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_MINUTES=10080

# PRODUCTION FREECAD
# FreeCAD binary path - container image'e gömülü
FREECADCMD_PATH=/usr/bin/freecadcmd
FREECAD_ASM4_REQUIRED=true
FREECAD_TIMEOUT_SECONDS=3600

# PRODUCTION AI INTEGRATION
# OpenAI API - rate limit ve billing farkındalığı ile
OPENAI_API_KEY=${SECRET_OPENAI_API_KEY}
# Azure OpenAI (tercihen)
AZURE_OPENAI_ENDPOINT=${SECRET_AZURE_OPENAI_ENDPOINT}
AZURE_OPENAI_API_KEY=${SECRET_AZURE_OPENAI_API_KEY}
AZURE_OPENAI_API_VERSION=2024-06-01

# PRODUCTION MODEL YÖNETİMİ
MODEL_POLICY=quality_first
DEFAULT_MODEL=gpt-4o
FALLBACK_MODEL=gpt-4o-mini
ALLOWED_MODELS=gpt-4o,gpt-4o-mini,o1-preview,o1-mini

# PRODUCTION OAUTH
# Google OAuth - Production app credentials
GOOGLE_CLIENT_ID=${SECRET_GOOGLE_CLIENT_ID}
GOOGLE_CLIENT_SECRET=${SECRET_GOOGLE_CLIENT_SECRET}

# PRODUCTION BRANDING
BRAND_NAME=CNC AI Suite
BRAND_LOGO_PATH=/app/static/logo.png
PDF_MAX_MB=50
PUBLIC_WEB_BASE_URL=${SECRET_PUBLIC_WEB_URL}

# PRODUCTION TOOLBITS
TOOLBITS_ROOT=/app/data/toolbits

# PRODUCTION FRONTEND
NODE_ENV=production
NEXT_PUBLIC_API_BASE_URL=${SECRET_PUBLIC_API_URL}
NEXT_PUBLIC_DEV_AUTH_BYPASS=false

# KUBERNETES & HEALTH CHECKS
# Production deployment health check endpoints
HEALTH_CHECK_PATH=/api/v1/healthz
READINESS_CHECK_PATH=/api/v1/ready
LIVENESS_CHECK_PATH=/api/v1/live

# PRODUCTION SECURITY HEADERS
# CORS, CSP ve güvenlik header'ları
CORS_ORIGINS=${SECRET_CORS_ORIGINS}
CSP_POLICY=${SECRET_CSP_POLICY}
HSTS_MAX_AGE=31536000

# RATE LIMITING
# Production API rate limiting
RATE_LIMIT_PER_MINUTE=60
RATE_LIMIT_PER_HOUR=1000
RATE_LIMIT_PER_DAY=10000

# LOGGING & AUDIT
# Production logging configuration
LOG_LEVEL=INFO
LOG_FORMAT=json
AUDIT_LOG_ENABLED=true
AUDIT_LOG_RETENTION_DAYS=90

# BACKUP & DISASTER RECOVERY
# Backup ve restore ayarları
BACKUP_SCHEDULE="0 2 * * *"
BACKUP_RETENTION_DAYS=30
BACKUP_S3_BUCKET=${SECRET_BACKUP_S3_BUCKET}

# COMPLIANCE & PRIVACY
# KVKK ve GDPR uyumluluk
GDPR_ENABLED=true
KVKK_ENABLED=true
DATA_RETENTION_DAYS=2555
PERSONAL_DATA_ENCRYPTION=true

# CERTIFICATE & TLS
# TLS sertifika ayarları
TLS_CERT_PATH=/etc/ssl/certs/app.crt
TLS_KEY_PATH=/etc/ssl/private/app.key
TLS_MIN_VERSION=1.2

# =================================================================================
# SECRET MANAGEMENT PLACEHOLDERS
# =================================================================================
# Bu değerler external secret management sistemi tarafından inject edilmelidir:
# - Doppler: doppler run -- ./start.sh
# - Vault: vault kv get -field=value secret/app/config
# - K8s Secrets: valueFrom.secretKeyRef in deployment.yaml
# - Azure Key Vault: @Microsoft.KeyVault(SecretUri=...)
# =================================================================================
